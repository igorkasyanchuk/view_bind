<div class="section-heading"><h2>The latest stories</h2><span><%= @posts.size %> stories · Newest first</span></div>
<section class="cards" aria-label="Latest stories"><%= bind_render_each "posts/card_bound", @posts, as: :post %></section>
