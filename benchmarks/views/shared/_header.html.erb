<header class="site-header">
  <div class="header-inner">
    <a class="brand" href="/?per=6"><span class="brand-mark" aria-hidden="true">b</span><span>the build<small>CODE. CRAFT. CULTURE.</small></span></a>
    <%= render "shared/nav", current_path: current_path %>
    <div class="user"><span class="user-name">Hello, <%= user %></span><%= render "shared/avatar", name: user %><%= render "shared/button", label: "Sign out", style: "ghost" %></div>
  </div>
</header>
